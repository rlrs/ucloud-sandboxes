diff --git a/storage/overlaybd/src/backend/cache/full_file_cache/cache_pool.rs b/storage/overlaybd/src/backend/cache/full_file_cache/cache_pool.rs
index 8621eed..73ea2cc 100644
--- a/storage/overlaybd/src/backend/cache/full_file_cache/cache_pool.rs
+++ b/storage/overlaybd/src/backend/cache/full_file_cache/cache_pool.rs
@@ -9,7 +9,6 @@ use super::cache_entry::CacheEntry;
 use super::cache_store::CachedFile;
 use crate::config::CacheConfig;
 use crate::io::virtual_file::VirtualFile;
-use crate::lsmt::file::PREMERGED_INDEX_DIR;
 use crate::sys;
 use anyhow::{anyhow, bail, Result};
 use dashmap::DashMap;
@@ -198,9 +197,9 @@ impl BackendState {
                 continue;
             }
             let cache_id = item.file_name().to_string_lossy().to_string();
-            // The LSMT premerged-index artifact cache shares this cache root
-            // but is not a full-file cache entry; leave it untouched.
-            if cache_id == PREMERGED_INDEX_DIR {
+            // Only hashed blob-entry directories belong to this cache. Other
+            // caches (notably premerged-index) share the configured root.
+            if cache_id.len() != 64 || !cache_id.bytes().all(|b| b.is_ascii_hexdigit()) {
                 continue;
             }
             let paths = EntryPaths::new(&options.cache_dir, &cache_id);
diff --git a/storage/overlaybd/src/backend/cache/tests.rs b/storage/overlaybd/src/backend/cache/tests.rs
index bf0fd3b..7b2fa49 100644
--- a/storage/overlaybd/src/backend/cache/tests.rs
+++ b/storage/overlaybd/src/backend/cache/tests.rs
@@ -2757,3 +2757,16 @@ async fn test_failed_foreground_refill_leaves_no_reserved_blocks() {
         "failed refill must leave the data file fully sparse"
     );
 }
+
+#[tokio::test]
+async fn test_file_cache_startup_preserves_sibling_cache_directories() {
+    let dir = tempdir().unwrap();
+    let sibling = dir.path().join("premerged-index");
+    std::fs::create_dir(&sibling).unwrap();
+    let artifact = sibling.join("existing.pmidx");
+    std::fs::write(&artifact, b"existing index").unwrap();
+    let mut options = FileCacheBackendOptions::default();
+    options.cache_dir = dir.path().to_path_buf();
+    let _backend = FileCacheBackend::with_options(options).await.unwrap();
+    assert_eq!(std::fs::read(artifact).unwrap(), b"existing index");
+}
diff --git a/storage/overlaybd/src/lsmt/file/helper.rs b/storage/overlaybd/src/lsmt/file/helper.rs
index 088f28d..a96485f 100644
--- a/storage/overlaybd/src/lsmt/file/helper.rs
+++ b/storage/overlaybd/src/lsmt/file/helper.rs
@@ -250,7 +250,7 @@ impl PremergedIndexCacheKey {
         }
 
         let mut key = Vec::new();
-        key.extend_from_slice(b"overlaybd-premerged-index-key-v1");
+        key.extend_from_slice(b"overlaybd-premerged-index-key-v2");
         append_u32(&mut key, PREMERGED_INDEX_FORMAT_VERSION);
         append_u32(&mut key, PREMERGED_INDEX_MERGE_VERSION);
         append_u32(&mut key, metadata.len() as u32);
@@ -263,6 +263,7 @@ impl PremergedIndexCacheKey {
             append_u64(&mut key, layer.virtual_size);
             append_u64(&mut key, layer.index_offset);
             append_u64(&mut key, layer.index_size);
+            key.extend_from_slice(&layer.index_digest);
             key.push(layer.header_version);
             key.push(layer.header_sub_version);
             key.push(layer.trailer_version);
@@ -1016,7 +1017,36 @@ async fn load_readonly_layer_metadata(file: Arc<dyn VirtualFile>) -> Result<Read
     let file_size = file.size().await?;
     let header = verify_ht(&file, false, file_size).await?;
     let trailer = verify_ht(&file, true, file_size).await?;
+    // UUID and file geometry identify the logical layer, not its physical
+    // representation. Dense export can reorder extents while preserving all
+    // of those fields. Bind cached physical offsets to the actual index bytes.
+    let index_len = trailer
+        .index_size
+        .get()
+        .checked_mul(size_of::<DiskSegmentMapping>() as u64)
+        .context("readonly index length overflow")?;
+    let index_end = trailer
+        .index_offset
+        .get()
+        .checked_add(index_len)
+        .context("readonly index end overflow")?;
+    ensure!(
+        index_end <= file_size - HEADER_SIZE,
+        "readonly index outside layer"
+    );
+    let mut hasher = Sha256::new();
+    let mut cursor = trailer.index_offset.get();
+    let mut scratch = vec![0u8; min(index_len, 64 * 1024) as usize];
+    while cursor < index_end {
+        let want = min(index_end - cursor, scratch.len() as u64) as usize;
+        let n = file.read_at_into(cursor, &mut scratch[..want]).await?;
+        ensure!(n == want, "short readonly index identity read");
+        hasher.update(&scratch[..want]);
+        cursor += want as u64;
+    }
+    let index_digest: [u8; 32] = hasher.finalize().into();
     Ok(ReadOnlyLayerMetadata {
+        index_digest,
         uuid: parse_uuid_field(&trailer.uuid).unwrap_or_else(Uuid::nil),
         file_size,
         virtual_size: trailer.virtual_size.get(),
diff --git a/storage/overlaybd/src/lsmt/file/tests.rs b/storage/overlaybd/src/lsmt/file/tests.rs
index 6ebbb3a..b57a268 100644
--- a/storage/overlaybd/src/lsmt/file/tests.rs
+++ b/storage/overlaybd/src/lsmt/file/tests.rs
@@ -618,18 +618,20 @@ impl VirtualFile for ShortReadFile {
     }
 }
 
-struct FailingIndexReadFile {
+struct SingleIndexReadFile {
     inner: Arc<dyn VirtualFile>,
     index_offset: u64,
     index_len: u64,
+    index_reads: AtomicUsize,
 }
 
-impl FailingIndexReadFile {
+impl SingleIndexReadFile {
     fn new(inner: Arc<dyn VirtualFile>, index_offset: u64, index_size: u64) -> Self {
         Self {
             inner,
             index_offset,
             index_len: index_size * size_of::<DiskSegmentMapping>() as u64,
+            index_reads: AtomicUsize::new(0),
         }
     }
 
@@ -641,10 +643,11 @@ impl FailingIndexReadFile {
 }
 
 #[async_trait]
-impl VirtualFile for FailingIndexReadFile {
+impl VirtualFile for SingleIndexReadFile {
     async fn read_at(&self, offset: u64, len: usize) -> Result<Bytes> {
         ensure!(
-            !self.overlaps_index(offset, len),
+            !self.overlaps_index(offset, len)
+                || self.index_reads.fetch_add(1, AtomicOrdering::SeqCst) == 0,
             "unexpected layer index read at offset {offset} len {len}"
         );
         self.inner.read_at(offset, len).await
@@ -652,7 +655,8 @@ impl VirtualFile for FailingIndexReadFile {
 
     async fn read_at_into(&self, offset: u64, dst: &mut [u8]) -> Result<usize> {
         ensure!(
-            !self.overlaps_index(offset, dst.len()),
+            !self.overlaps_index(offset, dst.len())
+                || self.index_reads.fetch_add(1, AtomicOrdering::SeqCst) == 0,
             "unexpected layer index read_at_into at offset {} len {}",
             offset,
             dst.len()
@@ -3010,6 +3014,7 @@ fn test_layer_metadata(uuid: Uuid, index_size: u64) -> ReadOnlyLayerMetadata {
         virtual_size: 512 * 1024,
         index_offset: 4096,
         index_size,
+        index_digest: [0; 32],
         header_version: 1,
         header_sub_version: 1,
         trailer_version: 1,
@@ -3040,6 +3045,15 @@ fn test_premerged_index_cache_key_metadata_identity() {
     let changed_key = PremergedIndexCacheKey::from_metadata(&changed).unwrap();
     assert_ne!(key0.digest, changed_key.digest);
 
+    let mut changed_layout = metadata.clone();
+    changed_layout[0].index_digest[0] = 1;
+    assert_ne!(
+        key0.digest,
+        PremergedIndexCacheKey::from_metadata(&changed_layout)
+            .unwrap()
+            .digest
+    );
+
     let mut nil_uuid = metadata;
     nil_uuid[0].uuid = Uuid::nil();
     assert!(PremergedIndexCacheKey::from_metadata(&nil_uuid).is_none());
@@ -3245,7 +3259,7 @@ async fn wait_readable_premerged_artifact(
 }
 
 #[tokio::test]
-async fn test_premerged_index_artifact_hit_skips_layer_index_load() {
+async fn test_premerged_index_artifact_hit_reads_index_identity_once() {
     let temp_dir = TempDir::new().unwrap();
     let vsize = 4 * 1024 * 1024;
     let base = create_sealed_layer(&temp_dir, "pm-base", vsize, &[(4096, 0x11)]).await;
@@ -3271,7 +3285,7 @@ async fn test_premerged_index_artifact_hit_skips_layer_index_load() {
         .cloned()
         .zip(metadata.iter())
         .map(|(file, layer)| {
-            Arc::new(FailingIndexReadFile::new(
+            Arc::new(SingleIndexReadFile::new(
                 file,
                 layer.index_offset,
                 layer.index_size,
@@ -3286,7 +3300,7 @@ async fn test_premerged_index_artifact_hit_skips_layer_index_load() {
     };
     let lower = open_files_ro_with_premerged_cache(&wrapped, &cache_dir, hit_policy)
         .await
-        .expect("artifact hit should avoid reading per-layer indexes");
+        .expect("artifact hit should read each index only for identity verification");
     assert_eq!(lower.read_at(4096, 4096).await.unwrap()[0], 0x11);
     assert_eq!(lower.read_at(8192, 4096).await.unwrap()[0], 0x22);
 }
@@ -4101,3 +4115,51 @@ async fn test_direct_io_create_sparse() {
     assert_eq!(lsmt.size().await.unwrap(), vsize);
     assert_eq!(lsmt.file_type(), LSMTFileType::SparseReadWrite);
 }
+
+#[tokio::test]
+async fn test_premerged_cache_distinguishes_dense_export_physical_offsets() {
+    let dir = TempDir::new().unwrap();
+    // Log-structured writes arrive out of logical order. Dense export sorts
+    // them while preserving UUID, size and index geometry.
+    let source = create_sealed_layer(
+        &dir,
+        "layout-source",
+        4 * 1024 * 1024,
+        &(0..256)
+            .rev()
+            .map(|i| (i * 8192, if i == 0 { 0x11 } else { 0x22 }))
+            .collect::<Vec<_>>(),
+    )
+    .await;
+    let original: Vec<Arc<dyn VirtualFile>> = vec![source.clone()];
+    let cache = dir.path().join("cache");
+    let policy = PremergedIndexCachePolicy::hybrid(1);
+    let first = open_files_ro_with_premerged_cache(&original, &cache, policy)
+        .await
+        .unwrap();
+    assert_eq!(first.read_at(0, 4096).await.unwrap()[0], 0x11);
+    wait_premerged_artifact_count(&cache, 1).await;
+    let dense = Arc::new(
+        LocalFile::new(dir.path().join("dense"))
+            .await
+            .unwrap(),
+    );
+    open_file_ro(source)
+        .await
+        .unwrap()
+        .commit_preserving_metadata(CommitArgs::new(dense.clone()))
+        .await
+        .unwrap();
+    let exported: Vec<Arc<dyn VirtualFile>> = vec![dense];
+    let old = load_readonly_layers_metadata(&original).await.unwrap();
+    let new = load_readonly_layers_metadata(&exported).await.unwrap();
+    assert_eq!(old[0].uuid, new[0].uuid);
+    assert_eq!(old[0].file_size, new[0].file_size);
+    assert_eq!(old[0].index_offset, new[0].index_offset);
+    assert_eq!(old[0].index_size, new[0].index_size);
+    let restored = open_files_ro_with_premerged_cache(&exported, &cache, policy)
+        .await
+        .unwrap();
+    assert_eq!(restored.read_at(0, 4096).await.unwrap()[0], 0x11);
+    assert_eq!(restored.read_at(8192, 4096).await.unwrap()[0], 0x22);
+}
diff --git a/storage/overlaybd/src/lsmt/file/types.rs b/storage/overlaybd/src/lsmt/file/types.rs
index 3dedde4..c95c6af 100644
--- a/storage/overlaybd/src/lsmt/file/types.rs
+++ b/storage/overlaybd/src/lsmt/file/types.rs
@@ -80,6 +80,7 @@ pub(super) struct ReadOnlyLayerMetadata {
     pub(super) virtual_size: u64,
     pub(super) index_offset: u64,
     pub(super) index_size: u64,
+    pub(super) index_digest: [u8; 32],
     pub(super) header_version: u8,
     pub(super) header_sub_version: u8,
     pub(super) trailer_version: u8,
